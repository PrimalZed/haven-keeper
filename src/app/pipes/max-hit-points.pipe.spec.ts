import { MaxHitPointsPipe } from './max-hit-points.pipe';

describe('MaxHitPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new MaxHitPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
